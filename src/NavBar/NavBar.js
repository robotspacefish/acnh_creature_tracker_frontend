import React from 'react';
import './NavBar.css';

const NavBar = props => (
  <div className="Nav">
    Login | Signup
    {/* todo React Router
    if !logged_in Login | Signup
    if logged_in My Creatures */}
  </div>
);

export default NavBar;