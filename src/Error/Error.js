import React from 'react';
import './Error.css';

const Error = () => (
  <div className="Error container">
    <h2>Oops, you've gone to a page that doesn't exist or you don't have access to</h2>
  </div>
);

export default Error;